<ul class="preview__list">
    {{#each this}}
    <li class="preview__item">
        <img class="preview__img" src="{{flag}}" alt="flag of {{name}}" width="100" height="100">
        <p class="preview__name">{{name}}</p>
    </li>
    {{/each}}
</ul>